# Fuente de datos del adapter (§8). `mock` no requiere proxy ni credenciales.
VITE_DATA_SOURCE=mock

# URL del proxy (Cloudflare Worker) cuando VITE_DATA_SOURCE=live.
VITE_PROXY_URL=http://localhost:8787

# Semilla opcional para mocks deterministas en tests (§8).
# VITE_MOCK_SEED=42
